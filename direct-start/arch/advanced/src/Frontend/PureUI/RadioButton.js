import * as MUI from "material-ui/RadioButton";
import { pure } from "recompose";

const RadioButton = pure( MUI.RadioButton );
const RadioButtonGroup = pure( MUI.RadioButtonGroup );

export { RadioButton, RadioButtonGroup };
