import React from 'react';
import makeWindow from 'renext-core/makeWindow';
import { view as UserManager } from '../Connected';
export default makeWindow( ( props ) => (
  <UserManager
    {...props}
    onCancel={() => {props.closeWindow()}}
    onSuccess={() => {props.onSuccess();props.closeWindow()}}
  />
));
